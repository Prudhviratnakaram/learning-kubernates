apiVersion: v1
kind: ReplicationController
metadata:
  name: nginx new
spec:
  replicas: 3
  selector:
    app: nginx
  template:
    metadata:
      name: nginx new
      labels:
        app: nginx
    spec:
      containers:
        - name: nginx
          image: nginx
